Launch loader validation: A fresh live session displayed the Monocraft Shopfront signal-boot loader with shuffled text, progress, and the retail-signage composition. A subsequent inspection confirmed that the loader exited cleanly and normal homepage controls became available. After the timing update, a fresh session still showed the loader at approximately 70–80% progress before the normal page appeared, confirming the extended three-second sequence.

CookeByte validation: Fresh sessions displayed the renamed CookeByte interface, intensified in-progress character scramble, and a visible sound control. The sandbox browser enforced autoplay policy and therefore exposed the explicit “Tap for sound” path, which is the intended user-gesture fallback when automatic launch audio cannot start.
